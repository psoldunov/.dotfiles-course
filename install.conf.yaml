- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    #     ~/.vim: vim
    #     ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/Work
# - shell:
#     - [git submodule update --init --recursive, Installing submodules]
